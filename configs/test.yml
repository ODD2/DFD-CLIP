tracking:
  enabled: true
  project_name: mix
  directory: logs/multi_evidence

model:
  out_dim: 
    - 180
    - 2
  losses:
    - kl_div
    - auc_roc

data:
  train:
    - name: rPPG
      dataset: RPPG
    - name: Deepfake
      dataset: FFPP
      types:
        - "REAL"
        - "DF"
  eval:
    - name: rPPG
      dataset: RPPG
      train_ratio: 0.999
    - name: Deepfake
      dataset: FFPP
      types:
        - "REAL"
        - "DF"

trainer:
  batch_size: 10
  learning_rate: 0.0003
  max_steps: 5
  num_workers: 8
  metrics:
    - name: rPPG
      types:
        - mse
    - name: Deepfake
      types:
        - accuracy
        - roc_auc


evaluator:
  batch_size: 8
  num_workers: 8
  metrics:
    - name: rPPG
      types:
        - mse
    - name: Deepfake
      types:
        - accuracy
        - roc_auc

system:
  deterministic_training: true
  training_eval_interval: 3
  evaluation_interval: 5

